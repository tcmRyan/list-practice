import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// This is the entry point for the application.  React will
// traverse the code from here to generate the application
// we are building.
ReactDOM.render(<App />, document.getElementById("root"));
